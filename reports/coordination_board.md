# Coordination Board

## Global Rules
- Sector3D must remain coreless axial-flux.
- Stator route is rigid PCB support + double-sided flat copper active conductors.
- Do not reuse iron-core assumptions.
- Do not commit AEDT runtime/generated outputs unless explicitly requested.

## Active Threads
| Thread | Branch | Owns | Status | Last Commit | Blockers |
|---|---|---|---|---|---|
| Integration | main | merge/review/coordination | active | current HEAD | no agent branch has unique commits to merge |
| Host Runtime | agent/host-runtime | host/queue/launchers | integrated | 0ab4a6a | no unique host-runtime commits pending in this Sector3D branch context |
| Sector3D Geometry | agent/sector3d-geometry | 3D geometry scaffold | active | 65f9702 | rotating-band geometry, revised working/template save flow, and oriented axial PM project materials are live-verified; geometry/material path no longer blocks solve readiness |
| Sector3D Solve | agent/sector3d-solve | excitation/setup/reports/solve | blocked | 8b31b49 | source/sink sheet angle placement is now corrected from build-artifact/config metadata when live AEDT variables read as expressions; assign-only rerun places sheets at the expected belt centers, but no winding/current excitation is usable yet (`PhaseA -> AssignWindingGroup`, `PhaseB/C -> CoilTerminal`) |
| Linear2D | agent/linear2d | 2D screening/ranking | waiting |  |  |
| DOE Ranking | agent/doe-ranking | search space/DOE/ranking | waiting |  |  |
| Docs Review | agent/docs-review | docs/contracts/review | waiting |  |  |

## Integration Review
- 2026-04-23: `git pull` and `git fetch --all --prune` found all local agent branches at `0ab4a6a` with no unique commits beyond `origin/main`.
- No merge performed this round because no agent branch had commits ahead of `main`.
- Physical constraint review found the active config still requires coreless axial-flux, rigid PCB support, double-sided flat copper active conductors, axial magnets, expanded air-region/fringing review, rotating band, and master/slave sector periodicity.
- Generated AEDT/report outputs remain untracked and must not be staged by integration: `reports/sector3d_model_build.md`, `reports/sector3d_excitation_assignment.md`, `templates/sector3d_template.aedt`, plus runtime/log/artifact/export/aedt project outputs.
- 2026-04-23 Sector3D Geometry: patched scaffold code to generate a 2-pole / 30-degree SSDR periodic-sector geometry instead of a full-annulus helper. Live AEDT rebuild wrote a pre-template-save `artifacts/sector3d_model_build.json` with `sector_geometry.geometry_scope = periodic_sector`.
- 2026-04-24 Sector3D Geometry: latest live build/apply artifacts show the Maxwell-compatible rotating band path is working end to end at the geometry/setup level: `artifacts/sector3d_model_build.json` recorded `motion_band_objects[0].role = rotating_band_double_rotor_container`, and `artifacts/sector3d_transient_setup.json` recorded `band_object_exists = true` and `motion_assigned = true`.
- 2026-04-24 Sector3D Geometry: latest local code patch removes the silent blank-project fallback for existing working `.aedt` files and stops queue builds from `SaveAs`-switching the active working project into the template path.
- 2026-04-24 Sector3D Geometry: fresh host rerun at `2026-04-24T04:40Z` reused `sector3d_working.aedt` for both build and transient-setup queue commands, kept `project_list = ["Project2", "sector3d_working"]`, copied the working project to `templates/sector3d_template.aedt` by file copy, and produced no new `OpenProject failed ... attempting blank-project fallback` / `Created new project` evidence in the host log.
- 2026-04-24 Sector3D Geometry: fresh host rerun at `2026-04-24T04:53Z` now auto-duplicates `Magnet, permanent, Neodymium N42SH` into `Auto3D_PM_Axial_PlusZ` / `Auto3D_PM_Axial_MinusZ`, sets the coercivity vectors to `+Z` / `-Z`, assigns them to the four SSDR magnet objects, and writes `artifacts/sector3d_model_build.json` with `magnet_assignment.assigned_ok = true`, `baseline_ready_for_solve = true`, and `physics_ready_for_validation = true`.
- 2026-04-24 Sector3D Solve: downstream live queue still blocks after geometry/material success. `artifacts/sector3d_excitation_assignment.json` shows no usable winding/current excitations, `logs/assign_sector3d_excitation_2026-04-24T04-54-45Z.log` shows `Project save failed` via gRPC `Save`, and the following `solve_sector3d_setup` / `create_sector3d_reports` commands then fail `OpenProject(working)` under the no-blank-fallback guard.
- 2026-04-24 Sector3D Solve: local solve-side patch now assigns Maxwell 3D excitations from the inner/outer radial conductor faces instead of whole solids, adds a PyAEDT project-save fallback for the solve scripts, and teaches `open_or_create_project()` to reuse an already-active working project before trying `OpenProject()`.
- 2026-04-24 Sector3D Solve: fresh queue evidence after the solve-side patch still shows a host-state blocker. `Queue-AssignSector3DExcitation.ps1` at `2026-04-24T05:29:53Z` failed in host preparation before the script body ran, `Queue-ProbeSession.ps1` at `2026-04-24T05:30:41Z` reported `active_project = null` and `project_list = []`, while the local Windows process title for `ansysedt.exe` still showed `sector3d_working - Sector3D` open in the GUI.
- 2026-04-24 Sector3D Solve: after a fresh host restart, `Queue-ProbeSession.ps1` at `2026-04-24T08:59:30Z` again showed `active_project = sector3d_working` and `project_list = ["Project2", "sector3d_working"]`, and `Queue-AssignSector3DExcitation.ps1` at `2026-04-24T08:59:52Z` entered the script body successfully.
- 2026-04-24 Sector3D Solve: the latest live excitation log narrows the first Maxwell-side failure to `AssignCoilTerminal` on `PhaseA_Coil_Pos_Terminal_001`; the same run then failed all project-save paths and the host heartbeat dropped back to `active_project = null`, after which `Queue-CreateSector3DReports.ps1` at `2026-04-24T09:05:44Z` and `Queue-SolveSector3DSetup.ps1` at `2026-04-24T09:06:18Z` both failed reopening `sector3d_working.aedt`.
- 2026-04-24 Sector3D Solve: another fresh host rerun at `2026-04-24T09:15:20Z` again showed `active_project = sector3d_working` and `project_list = ["Project2", "sector3d_working"]`. The following `Queue-AssignSector3DExcitation.ps1` at `2026-04-24T09:15:43Z` proves the cached fallback now survives the first `AssignCoilTerminal` failure long enough to reach a real Maxwell `AssignCurrent` call on `PhaseA_Current_Pos_001`, but that boundary is still rejected and the same run again ends with `Save` failure and host-state loss.
- 2026-04-24 Sector3D Solve: the newest local excitation patch now pre-caches terminal-face specs for all three phases before any boundary creation and retries fallback direct current as `cached face -> cached object` with the A-Phi internal-conductor excitation model. This patch is locally regression-checked and `py_compile`-clean, but still needs one more fresh-host rerun because the `09:15Z` excitation failure dirtied the host before it could be live-validated.
- 2026-04-24 Sector3D Solve: fresh host rerun at `2026-04-24T09:24:14Z` again showed `active_project = sector3d_working` and `project_list = ["Project2", "sector3d_working"]`. The following `Queue-AssignSector3DExcitation.ps1` at `2026-04-24T09:24:44Z` live-validated the new all-phase pre-cache path: `PhaseB` and `PhaseC` no longer fail terminal discovery with `GetObjectsInGroup`, and all three phases now reach fallback current assignment.
- 2026-04-24 Sector3D Solve: that same `09:24Z` excitation run also shows the next narrowed blocker. For each phase, `AssignCoilTerminal` still fails first, and the fallback `AssignCurrent` is then rejected on both retry branches (`cached face` and `cached object`). The run still ends with `Save` failure and the host dropping back to `active_project = null`, so reports/solve remain blocked behind excitation API compatibility rather than project-open drift.
- 2026-04-25 Sector3D Solve: after another in-AEDT host restart, heartbeat at `2026-04-25T03:31:26Z` showed `active_project = sector3d_working`, `project_list = ["sector3d_working"]`, and `worker_state = idle`. Only `Queue-AssignSector3DExcitation.ps1` was queued, per the round constraint.
- 2026-04-25 Sector3D Solve: command `73531a530e3e43e0a4f444596fd2be19` finished at `2026-04-25T03:32:05Z` with `prepared = true` and `script_complete`. The fresh log repeats the same narrowed blocker: all three phases hit `AssignCoilTerminal` failure first, then fallback `AssignCurrent` fails on both cached face and cached object. No current excitations were created, save still fails, and host heartbeat again drops to `active_project = null`.
- 2026-04-25 Sector3D Solve: primitive review found `CurrentDensity`/`CurrentDensityTerminal` are not valid for Maxwell 3D `Transient` in the installed PyAEDT path, `assign_winding` still depends on `AssignCoilTerminal`, and stranded winding does not remove the terminal requirement. The local solve-side patch now creates explicit `Auto3D_SourceSink_*` sheet objects at each flat-copper segment's radial source/sink end and uses those sheet objects for `AssignCoilTerminal`, with fallback diagnostics ordered as `sheet -> cached face -> cached object`.
- 2026-04-25 Sector3D Solve: local regression checks and `py_compile` passed after the source/sink sheet patch, but no new assign queue was run because the current in-AEDT heartbeat is not fresh/usable: `active_project = null`, `project_list = []`, `worker_state = idle`.
- 2026-04-25 Sector3D Solve: after a fresh host restart, command `97fd859c536d457aa5dc6948f3095a1e` ran only `Queue-AssignSector3DExcitation.ps1`. It created 24 `Auto3D_SourceSink_*` sheets and PhaseA no longer failed at `PhaseA_Coil_Pos_Terminal_001`; the first PhaseA winding path failure moved to `AssignWindingGroup PhaseA_Winding`. PhaseB/C still fail at first `CoilTerminal`, fallback current still fails for all phases, no excitations are recorded, save still fails, and the host drops back to `active_project = null`.
- 2026-04-25 Sector3D Solve: the same run exposed a reused-model metadata issue: `phase_belt_angle_deg`, `phase_belt_gap_deg`, and `phase_segment_angle_deg` read back as `0.0`, so all source/sink sheets were placed at `-15deg`. Next fix should make source/sink sheet placement derive angles from sector/artifact/config metadata when AEDT design variables are symbolic expressions.
- 2026-04-25 Sector3D Solve: patched `assign_sector3d_excitation.py` so source/sink sheet angle placement falls back from live AEDT numeric values to `artifacts/sector3d_model_build.json`, then to `config/project.json`. Local red/green regression and `py_compile` passed.
- 2026-04-25 Sector3D Solve: fresh assign-only command `3a1bbfa9db67446095747843a90f4ed7` records `phase_belt_angle_source = build_artifact`, `phase_belt_angle_deg = 5.0`, `phase_belt_gap_deg = 0.05`, and source/sink sheets at the expected belt centers (`-12.5`, `-7.5`, `-2.5`, `2.5`, `7.5`, `12.5`). Excitation creation still fails: PhaseA reaches `AssignWindingGroup PhaseA_Winding`, PhaseB/C still fail at first `CoilTerminal`, fallback current fails, save fails, and the host drops to `active_project = null`.
